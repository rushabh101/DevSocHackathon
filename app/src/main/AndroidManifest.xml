<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devsochackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DevSocHackathon">
        <activity android:name=".Help"></activity>
        <activity android:name=".Reward" />
        <activity android:name=".unlocked_screen" />
        <activity android:name=".fail_screen" />
        <activity android:name=".Hard" />
        <activity android:name=".Easy" />
        <activity android:name=".Medium" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MazeActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".SplashActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>